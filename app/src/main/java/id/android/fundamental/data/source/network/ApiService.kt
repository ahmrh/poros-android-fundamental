package id.android.fundamental.data.source.network

import retrofit2.*

interface ApiService {

    // TODO 2: Buat fungsi untuk mengambil data dari tomorrow.io API menggunakan retrofit
    /**
        Contoh:
        @GET("route")
        fun getSomething(
            @Query("parameter") parameter: DataType,
        ): Call<ApiResponse>

        @GET("route/{id}")
        fun getSomething(
            @Path("id") id: DataType,
        ): Call<ApiResponse>
     */
}